# Forward all traffic to the Tor SOCKS5 proxy
forward-socks5t / 0.0.0.0:9050 .
# Listen on the following address and port
listen-address 0.0.0.0:8118
# Logging configuration
# logfile logfile
# debug 1