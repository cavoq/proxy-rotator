# proxy-rotator

Modern rotating TOR proxy.

## Usage

**Build the container:**
```bash
docker build -t proxy-rotator .
```

**Run the container:**

```bash
docker run -it -p 8080:8080 proxy-rotator
```